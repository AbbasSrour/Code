#include <stdio.h>
#include <stdlib.h>

typedef struct node {
  int data;
  struct node *next;
} node;

void tailpush(node *node, int data) {
  node *newNode = (node *)newNode(sizeof(node));
}

int main(void) {}
